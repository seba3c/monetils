# Agent Context for monetils

`monetils` is a Python utility library. Source lives in `src/monetils/`, tests in `tests/`.

## Setup & Tests

```bash
uv sync
uv run pytest
```

## Lint & Format

```bash
uv run ruff check --fix .
uv run ruff format .
```

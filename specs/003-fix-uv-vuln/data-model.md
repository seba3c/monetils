# Data Model: Uv Dependency Vulnerability Remediation

Not applicable. This feature introduces no entities, fields, relationships, or state transitions — it is a single-file dependency-lockfile version bump (`uv.lock`), with no data model impact.
